# Google Analytics 4 measurement ID. Optional — leave empty and no analytics
# script is loaded at all. Copy this file to .env to use it locally, and set the
# same variable in Vercel → Project → Settings → Environment Variables.
VITE_GA_ID=
